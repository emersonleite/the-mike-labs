---
const { name, photo, bio, dark = false } = Astro.props;
---

<div
  class={`rounded-2xl p-6 text-center shadow-md border transition-colors duration-300
  ${dark ? 'bg-dark-2 border-border text-white' : 'bg-card border-border text-text'}`}
>
  <img src={photo} alt={name} class="w-28 h-28 mx-auto rounded-full object-cover mb-4 border-4 border-primary" />
  <h3 class="text-xl font-semibold text-primary">{name}</h3>
  <p class={`text-md mt-2 leading-relaxed font-sans-2 ${dark ? 'text-muted' : 'text-text'}`}>
    {bio}
  </p>
</div>
